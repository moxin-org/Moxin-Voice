## 1. 分类与数据配置更新

- [x] 1.1 定位 `select voice` 使用的声音特质分类配置源，移除 `童声` 分类定义与相关映射。
- [x] 1.2 在分类配置中新增 `专业播音`、`特色人物`，并显式定义展示顺序。
- [x] 1.3 配置 `专业播音` 对应声音集合：`白岩松`、`罗翔`、`沈逸`。
- [x] 1.4 配置 `特色人物` 对应声音集合：`马云`、`杨幂`、`周杰伦`、`trump罗翔`。

## 2. 页面渲染与状态处理

- [x] 2.1 更新分类渲染逻辑，确保 UI 只展示当前配置中的有效分类。
- [x] 2.2 更新分类筛选逻辑，确保各分类只显示其映射声音，不混入其他条目。
- [x] 2.3 在初始化/状态恢复路径加入兜底：若命中已删除分类（如 `童声`）则回退到默认有效分类。

## 3. 验证与回归

- [x] 3.1 增加或更新测试用例，覆盖“童声不显示”“新分类显示”“分类内声音集合正确”。
- [x] 3.2 增加或更新测试用例，覆盖“持久化为无效分类时自动回退”。
- [ ] 3.3 手动回归 `select voice`：分类切换、声音加载、声音选择流程无回归。
