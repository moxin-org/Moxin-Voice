## 1. UI 与交互

- [x] 1.1 在语音设置区域新增“输出音色”模块，包含语速与音调两个配置项
- [x] 1.2 为语速与音调实现离散选项控件（如高/中/低、快/中/慢）并展示选中态
- [x] 1.3 补充首次进入与状态缺失场景的默认值展示逻辑

## 2. 状态与参数映射

- [x] 2.1 在前端状态模型中新增语速与音调字段，并在初始化时填充默认值
- [x] 2.2 实现语速与音调到语音请求参数的映射表与转换函数
- [x] 2.3 在语音生成请求构建流程中接入映射结果，并对空值/越界值执行回退

## 3. 验证与回归

- [x] 3.1 添加或更新测试用例，覆盖默认值、选项切换、参数透传与异常回退
- [x] 3.2 进行手工回归，验证 UI 展示、选中态同步和不同档位下的语音输出行为
- [x] 3.3 更新相关说明文档（参数含义、默认行为、已知限制）
