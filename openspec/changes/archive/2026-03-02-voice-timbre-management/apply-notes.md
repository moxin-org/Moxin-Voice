## 回归验证记录

执行时间：2026-03-02

### 已执行测试

1. `cargo test -p mofa-tts --lib timbre -- --nocapture`
- 结果：通过（2/2）
- 覆盖点：
  - 标签枚举合法/非法值校验
  - 组合筛选逻辑（组内 OR、组间 AND）

2. `cargo test -p mofa-tts --lib backfill -- --nocapture`
- 结果：通过（2/2）
- 覆盖点：
  - 历史音色缺失标签回填
  - 回填后标签校验

3. `cargo test -p mofa-tts --lib`
- 结果：12 通过 / 1 失败
- 失败项：`voice_persistence::tests::test_voice_id_with_chinese`
- 说明：该失败与本次音色标签/筛选改动无直接耦合，属于既有 ID 生成断言行为问题。

## 发布步骤

1. 部署包含本次变更的 `mofa-tts` 版本。
2. 打开“音色库”页面，验证：
- 可见筛选项：男声/女声/童声、甜美/磁性/青年音。
- 单维度多选行为为 OR。
- 跨维度组合行为为 AND。
- 清空筛选后恢复列表。
3. 创建或更新自定义音色，确认 `gender_age`、`style` 被持久化。
4. 加载历史音色（无标签）确认可展示且不报错，并在保存后自动回写标签。

## 回滚步骤

1. 回滚到上一版本二进制。
2. 若需热修复：临时隐藏音色库筛选入口（仅保留搜索）。
3. 保留新增字段数据不做破坏性迁移，旧版本读取不依赖该字段。
4. 复核 `custom_voices.json` 可读性与核心生成链路（TTS 合成、预览、删除）状态。
