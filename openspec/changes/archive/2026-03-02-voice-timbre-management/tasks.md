## 1. 数据模型与兼容层

- [x] 1.1 在音色实体与持久化结构中新增 `gender_age`、`style` 字段，并定义受控枚举值（male/female/child, sweet/magnetic/youth）。
- [x] 1.2 在创建与更新音色接口中加入标签值校验，非法值返回明确错误。
- [x] 1.3 在读取链路实现历史数据兼容策略：缺失标签时返回默认值或“未分类”标识。
- [x] 1.4 在音色编辑保存流程中补齐并回写历史数据标签字段。

## 2. 筛选能力实现

- [x] 2.1 在音色管理界面新增性别年龄筛选项（男声/女声/童声）并映射到枚举值。
- [x] 2.2 在音色管理界面新增风格筛选项（甜美/磁性/青年音）并映射到枚举值。
- [x] 2.3 实现筛选组合逻辑：维度内 OR、维度间 AND，并在筛选变更时实时刷新列表。
- [x] 2.4 实现筛选清空行为，保证可恢复到“显示全部（受其他条件影响）”状态。

## 3. 质量保障与发布

- [x] 3.1 增加模型校验测试：覆盖合法值写入与非法值拒绝场景。
- [x] 3.2 增加兼容测试：覆盖历史无标签数据读取与编辑后回写场景。
- [x] 3.3 增加筛选测试：覆盖单条件筛选、多选 OR、跨维度 AND 与清空筛选。
- [x] 3.4 执行回归验证并记录发布/回滚步骤，确认旧版本兼容性与风险缓解措施。
